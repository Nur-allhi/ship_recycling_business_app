
// This file contains the Base64 encoded font files for PDF generation.
// This is necessary to ensure that the fonts are available to jsPDF when it runs.

// Roboto Slab Regular
export const robotoSlabBase64 = "";

// Trio Bangla Regular (This is a placeholder as I cannot generate Bangla fonts)
// For a real application, this would need the actual Base64 of the Trio Bangla font file.
// For now, we will re-use Roboto Slab for both to ensure PDF generation is successful.
export const trioBanglaBase64 = "";
